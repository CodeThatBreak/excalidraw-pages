# Use the official npm registry
registry=https://registry.npmjs.org/

# Save exact versions of packages to package.json
save-exact=true

# Set custom cache directory
cache=./.npm-cache

# Set custom global installation directory
prefix=./.npm-global    